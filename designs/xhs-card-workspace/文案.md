# Card Workspace · 小红书发布文案

配套图：`小红书宣传卡片.html`（4 张 3:4，1242×1656）

---

## 标题（3 选 1，控制在 20 字内）

1. Obsidian 的文件列表，我实在扫不动了
2. 把 Obsidian 文件夹变成卡片流，终于愿意回头翻笔记
3. 写了个 Obsidian 插件：文件夹直接变卡片流

---

## 正文

用 Obsidian 两年，最大的问题不是没记，是记完找不回来。

左边那栏文件列表只有文件名。一个文件夹几十篇，光看名字根本想不起里面写了什么，只能一篇篇点开确认——点到第三篇就懒了。

所以我自己写了个插件：Card Workspace。它把文件夹里的笔记铺成卡片流，每张卡上是标题加一段自动抽出来的摘要，扫一眼就知道要不要打开。

每天真正用到的是这三点：

1｜双栏侧边栏。文件夹、标签、卡片盒、收藏都在同一栏里，切换一次点击，不用来回跳文件浏览器。

2｜卡片盒。散在不同文件夹里但主题相关的笔记，用「文件夹 + 标签」的规则聚成一个集合。不搬文件，也不用手动维护索引笔记。

3｜拖进编辑器就是链接。卡片拖到正文里，可以插 wikilink、嵌入，或者直接把内容带过去。

另外还有全文搜索、标签筛选、置顶、批量移动/删除/合并——都在面板里，不用离开当前位置。

插件市场直接搜 Card Workspace 就能装。仅桌面端，需要 Obsidian 1.9.0 以上；所有处理都在本地，不发任何网络请求，MIT 开源免费。

你在 Obsidian 里最想要什么功能？评论区说一声，下个版本我看着加。

---

## 标签

#obsidian #obsidian插件 #笔记软件 #效率工具 #知识管理 #双链笔记 #数字花园 #独立开发

---

## 评论区话术

**置顶评论**

插件市场搜 Card Workspace 就能装，桌面端专用（手机上用不了，抱歉）。有问题直接在这条下面问，我都看。

**「和 XX 卡片插件有什么区别？」**

它长在左侧边栏里，浏览和编辑同屏；而且卡片盒是规则驱动的，不用手动维护列表。

**「支持移动端吗？」**

暂时不行。整套交互是围绕桌面端的双栏侧边栏做的。

**「会一直更新吗？」**

我自己每天在用，所以会。想要什么功能这条下面留言。

---

## 发布前待补的截图

图位在 HTML 里拖入图片即可替换（按住拖动平移，滚轮缩放，自动存本地）。

| 图位 | 卡片 | 需要什么 |
|---|---|---|
| `cover` | 01 封面 | 卡片流全貌，深色主题更出片 |
| `cmp-list` | 02 对比 | **Obsidian 原生文件浏览器**（只有文件名的那栏） |
| `cmp-cards` | 02 对比 | 同一个文件夹在 Card Workspace 里的卡片流 |
| `feature-shot` | 03 功能 | 双栏导航 + 卡片盒区域 |
| `cta-shot` | 04 结尾 | 插件市场里搜索 Card Workspace 的结果 |

02 的对比图是这篇的说服力来源：两张截图要取**同一个文件夹**，宽度和主题保持一致。
