Task 5 edge-scenario evidence

Exact command:
`npx vitest run src/view/CardItem.svelte.test.ts -t "supports keyboard and context menu actions"`

Result:
- PASS: exactly named edge scenario test executed successfully.
- PASS: keyboard Enter action still prevents default and triggers open-note callback payload semantics.
- PASS: context menu action still prevents default and sends `{ path, mouseEvent }` semantics through callback props.
- PASS: runtime test seam no longer depends on deprecated component mutation/teardown APIs (`$set`, `$destroy`).
