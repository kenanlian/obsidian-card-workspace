Task 6 cleanup invariant evidence (2026-04-18)

Target scenario:
- `src/view/Toolbar.svelte.test.ts` test name preserved exactly: `cleans up menus on unmount`.

Behavior verified:
- Repeated mount/unmount cycles remove portal menus from `document.body`:
  - `.fce-filter-menu` -> no orphan after unmount
  - `.fce-folder-menu` -> no orphan after unmount
  - `.fce-sort-menu` -> no orphan after unmount
- Capture-phase document click listener lifecycle remains balanced:
  - addEventListener("click", ..., true) count > 0
  - removeEventListener("click", ..., true) count equals add count

Executed checks:
- PASS: `npx vitest run src/view/Toolbar.svelte.test.ts -t "cleans up menus on unmount"`
- PASS: `npx vitest run src/view/Toolbar.svelte.test.ts`
