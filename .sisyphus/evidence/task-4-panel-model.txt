Task 4 evidence — typed panel model bridge

Exact Task 4 QA command:
`grep -R "interface PanelModelState\|createPanelModel" src/view/panel-model.ts src/view/FolderCardView.ts && ! grep -R "\$set(" src/view/FolderCardView.ts && npx vitest run src/view/FolderCardView.test.ts -t "mounts panel and propagates updates"`

Result:
- PASS: command succeeded.
- PASS: `PanelModelState` and `createPanelModel` were found in `src/view/panel-model.ts` and `src/view/FolderCardView.ts`.
- PASS: no `$set(` matches in `src/view/FolderCardView.ts`.
- PASS: `mounts panel and propagates updates` test passed.

Additional required Task 4 checks (same run window):
- `npx vitest run src/view/card-context-actions.test.ts` -> PASS (87 passed)
- `npm test` -> PASS (235 passed)
- `npm run check` -> PASS
- `npm run build` -> PASS (pre-existing Toolbar a11y warnings unchanged)
