Task 4 legacy surface audit

Exact legacy-surface audit command:
`grep -R "panelModel" src/view/FolderCardView.ts src/view/FolderCardPanel.svelte src/__mocks__/FolderCardPanel.svelte.ts && grep -R "\$on(" src/view/FolderCardView.ts && grep -R "\$destroy()" src/view/FolderCardView.ts && ! grep -R "\$set(" src/view/FolderCardView.ts`

Result:
- PASS: `panelModel` references found in host, panel, and node mock seam files.
- PASS: host still owns callback normalization via `$on(...)` wiring in `FolderCardView`.
- PASS: host lifecycle teardown still uses `$destroy()` in `FolderCardView`.
- PASS: no `$set(` usage in `FolderCardView`.

Audit conclusion:
- Bridge remains thin (mount/unmount + callback normalization only).
- State updates flow through host-owned typed model mutations (`panelModel.mutate(...)`).
