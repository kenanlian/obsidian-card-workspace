Task 8 Evidence — Preview propagation integration regressions
Date: 2026-04-16

Command:
`npx vitest run src/view/card-context-actions.test.ts -t "settings-change previewLines refresh keeps sort/filter/includeSubfolders panel props stable|hydrateRange keeps sparse content non-empty while empty markdown remains empty"`

Result:
- PASS `src/view/card-context-actions.test.ts` (2 tests passed, 57 skipped)
- Duration: 502ms

Covered regressions:
- `previewLines` propagation after `settings-change` refresh in live `FolderCardView` hydration path
- Non-regression for panel state props during refresh (`sortField`, `sortDirection`, `activeFilterTags`, `pinnedPaths`, `includeSubfolders`)
- Stable sparse-vs-empty behavior (`single real preview line` => non-empty mode, whitespace-only => empty mode)
