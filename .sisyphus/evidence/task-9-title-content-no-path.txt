Task 9 evidence: title/content semantics without path-token matching

Updated tests:
- src/search/SearchIndexManager.test.ts

Validated behavior:
- Markdown documents match by title and by body content.
- Markdown folder/path tokens do not produce matches on their own.
- Supported non-Markdown files such as Assets/Project Brief.pdf are searchable by basename/title text only.
- Non-Markdown path-folder tokens like "Assets" do not match.
- Non-Markdown content-like terms do not match because content is not indexed for those files.
- Current MiniSearch tie ordering is preserved for equal-score results.

Focused verification:
- npx vitest run src/search/SearchIndexManager.test.ts -t "markdown title and markdown content"
- npx vitest run src/search/SearchIndexManager.test.ts -t "title-only"

Full verification:
- npm run check
- npm run build
- npm test

Result:
- Passed locally on 2026-05-08.
