Task: 2. Sparse vs explicit-empty regression coverage
Date: 2026-04-16
File: src/view/markdown-utils.test.ts

Added sparse/empty distinction tests:
- Sparse one-line remains standard preview:
  - "keeps sparse one-line content as standard text preview" (lines 170-174)
  - Expects mode "text" and exact HTML "<p>Only one truthful line.</p>"

- Sparse two-line remains standard preview:
  - "keeps sparse two-line content as standard text preview" (lines 176-180)
  - Expects mode "text" and exact HTML "<p>First truthful line. Second truthful line.</p>"

- Explicit non-previewable image/embed-only remains empty:
  - "treats image-only note as explicit empty" (lines 182-186)
  - "treats embed-only note as explicit empty" (lines 188-192)
  - Expects mode "empty" and empty HTML

- Frontmatter + later text remains non-empty:
  - "renders later body text after frontmatter, not empty" (lines 194-199)
  - Expects mode "text" with concrete rendered paragraph

Verification note:
- This suite explicitly separates truthful sparse content (`mode = text`) from explicit empty cases (`mode = empty`).
