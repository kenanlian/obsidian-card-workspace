2026-05-09: Search-layer match counts now ride on manager/service runtime metadata instead of scores. `SearchIndexManager.search()` preserves MiniSearch ordering in `orderedPaths`, computes case-insensitive non-overlapping literal counts from cached `documentsByPath` using unique whitespace-normalized query tokens over `title + content`, and `IndexedSearchService` only forwards counts for returned `indexed-ready` paths.
2026-05-09: Tightened Task 1 acceptance fixtures so tests prove the exact plan examples: `title="Alpha"` plus content with two `alpha` occurrences yields `matchCountsByPath[path] === 3` for query `alpha`, while a separate `alpha beta alpha` assertion still proves duplicate-token dedupe; the non-overlap fixture now isolates counted text basis `aaa` so query `aa` returns `1`.
2026-05-09: Panel-state threading for match counts stays isolated from cards and settings. `FolderCardView` owns private runtime `searchMatchCountsByPath`, copies it into every `PanelModelState` push, clears it on query reset/snapshot/load invalidation, and only promotes `result.matchCountsByPath` for current `indexed-ready` results so stale async work cannot leak counts.

### Task 3: Render Omnisearch-style title-row badge in card UI
- The existing components `CardItem.svelte` and `FolderCardPanel.svelte` have already been migrated to use Svelte 5 runes (`$props()`, `$derived()`), so `export let` cannot be used. We simply added `searchMatchCount` to the `CardItemProps` interface and destructured it with a default value.
- Added a `.fce-card-search-count` CSS class with `font-size: 0.7rem`, `color: var(--fce-text-muted)`, `white-space: nowrap`, and `flex: 0 0 auto` to prevent it from shrinking inside the title row.
- The `searchMatchCount` badge was conditionally rendered adjacent to the title header, successfully extracting count mapped from `PanelModelState.searchMatchCountsByPath[card.path] ?? 0`.

### Task 3 Style Cleanup
- Double-quoted strings are preferred across Svelte logic, properties, and TS query selectors in this repo.
- The `Array.from().find()` DOM query iteration required explicit typing `(c: Element) => ...` under strict mode to ensure robustness.
2026-05-09: Task 4 audit found most cross-layer count coverage already present, so the hardening stayed narrow: added case-insensitive candidate-bounded manager/service regressions, a cross-layer view test proving badges can render from runtime count metadata even when visible preview text omits the term, and a panel guard that suppresses stale badges whenever search is blocked/unavailable or effectively cleared.
2026-05-09: Final validation confirmed the badge work stayed within guardrails: NoteCardRecord remains count-free, pipeline projection still only filters/reorders by path, and CardItem renders only the provided runtime count. The only repo validation output was existing Svelte accessibility warnings in Toolbar.svelte, and they did not block check/build/test.
2026-05-09: Final F3 QA used the repo's jsdom/Vitest harness as the closest executable user-facing path because this Obsidian plugin workspace does not expose a runnable browser app for Playwright. `FolderCardPanel.svelte.test.ts` proved visible/suppressed `.fce-card-search-count` states, and `FolderCardView.test.ts` proved badges can render from indexed full-document counts even when the visible preview omits the query text.
2026-05-09: Final code-quality review approved the search hit count badge implementation. The maintainable pattern to preserve is: compute counts once in `SearchIndexManager`, filter them in `IndexedSearchService`, keep them in `FolderCardView` runtime state only, and clear them on every blocked, reset, or stale async path before pushing `PanelModelState`.
2026-05-09: Final scope review should treat `.sisyphus/boulder.json` as orchestration state and should verify that any preexisting stale evidence text for another plan is superseded in-place rather than misclassified as product scope drift.
2026-05-09: F1 plan compliance audit approved search-hit-count-badges. The decisive evidence was that counts are computed in `SearchIndexManager` from cached `documentsByPath` title+content, threaded through `searchMatchCountsByPath`, rendered only as a `CardItem` prop, and kept out of `NoteCardRecord`/pipeline behavior.
