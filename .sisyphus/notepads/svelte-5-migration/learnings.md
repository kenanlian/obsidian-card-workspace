- 2026-04-18: Added a split Vitest project setup in `vitest.config.ts`: `node` lane keeps existing `src/**/*.test.ts` behavior and mock aliasing, while a `jsdom` lane is reserved for future `src/**/*.svelte.test.ts` files.
- 2026-04-18: Scoped the `./FolderCardPanel.svelte` alias to the node lane only so legacy host-style tests keep using `src/__mocks__/FolderCardPanel.svelte.ts`, while jsdom component tests can import real Svelte components.
- 2026-04-18: Added `@sveltejs/vite-plugin-svelte` with `compilerOptions.compatibility.componentApi = 4` in Vitest config to keep migration baseline aligned with current legacy compile expectations.
- 2026-04-18: Added scripts `check:svelte`, `test:node`, and `test:component` to create explicit verification entry points without replacing existing `test`/`test:watch` workflows.
- 2026-04-18: Real `.svelte.test.ts` runtime tests for `CardItem.svelte` and `Toolbar.svelte` work in the jsdom lane when that project explicitly applies the Svelte plugin and `resolve.conditions = ["browser"]` in `vitest.config.ts`.
- 2026-04-18: Plan QA commands that use `vitest -t` require literal/regex name matches; keeping scenario phrases in test names (for example `opens note and toggles pin` and `cleans up menus on unmount`) prevents accidental `0 matched` skips.
- 2026-04-18: Added real root-panel runtime coverage in `src/view/FolderCardPanel.svelte.test.ts` (jsdom lane) that verifies empty-state text, populated `.fce-list` rendering, and numeric `hydrate-range` dispatch from the mounted Svelte runtime.
- 2026-04-18: Added host-contract coverage in `src/view/FolderCardView.test.ts` and routed this single host test file to jsdom in `vitest.config.ts`, preserving node-lane mock aliasing for legacy `card-context-actions.test.ts` while exercising real `FolderCardPanel.svelte` in host-open/close cycles.

- 2026-04-18: Task 3 host QA uses exact `vitest -t` phrase matching; naming the host scenario test exactly `mounts panel and propagates updates` prevents zero-match skips while keeping the same runtime assertions.
- 2026-04-18: Task 4 introduced a host-owned typed panel model (`src/view/panel-model.ts`) and switched `FolderCardView` panel updates from `$set` to `panelModel.mutate(...)`, with the outer bridge limited to mount/unmount and callback wiring.
- 2026-04-18: `FolderCardPanel.svelte` can consume a `panelModel` via subscribe/unsubscribe while still preserving existing exported props as a temporary compatibility surface.
- 2026-04-18: Node-lane host seam tests stay stable by capturing panel-model snapshots in mocks (`modelSnapshots`) instead of asserting direct `$set` call history.
- 2026-04-18: Task 4 repair verification confirms bulk-selection-only paths stay on `pushSelectionState()` (no `deriveVisibleCards()`/`deriveAvailableTags()` hot-path recompute) while `onOpen()` seeds the initial panel-model snapshot with current host bulk state.
- 2026-04-18: Task 5 migrated `CardItem.svelte` to rune contracts with `let { ... } = $props()`, `$derived(...)` for `isPinned`, and typed callback props (`onOpenNote`, `onBulkSelectCard`, `onCardContextMenu`, `onPinToggle`) replacing `createEventDispatcher`.
- 2026-04-18: Task 5 kept `FolderCardPanel.svelte` as the compatibility bridge by mapping CardItem callback props back to existing outward panel events (`open-note`, `bulk-select-card`, `card-context-menu`, `pin-toggle`) so host semantics remain unchanged.
- 2026-04-18: Task 5 component tests no longer rely on `component.$on`; `CardItem.svelte.test.ts` now validates real DOM interactions through callback props and includes the exact edge test name `supports keyboard and context menu actions` for plan QA filtering.
- 2026-04-18: Task 5 deprecation cleanup replaced `CardItem.svelte` `on:*` directives with event attributes and switched `CardItem.svelte.test.ts` from `$set`/`$destroy` to `mount`/`unmount` + remounting for pinned-state assertions, keeping callback payload contracts unchanged.
- 2026-04-18: Task 6 migrated `Toolbar.svelte` to `$props()` + callback props with `$derived` for computed summaries/arrays and `$effect` only for side-effect sync (filter draft tags and folder-menu expansion state), while preserving legacy payload contracts.
- 2026-04-18: Task 6 kept `FolderCardPanel.svelte` as the immediate compatibility bridge by mapping Toolbar callback props back to unchanged outward panel events (`toolbar-action`, `sort-change`, `filter-change`, `include-subfolders-change`, `select-folder`) so host tests (`card-context-actions`) stay behaviorally stable.
- 2026-04-18: Task 6 `Toolbar.svelte.test.ts` migration pattern mirrors Task 5: `mount`/`unmount` lifecycle + callback capture assertions, with the exact cleanup scenario name `cleans up menus on unmount` retained for plan QA `vitest -t` filtering.

- 2026-04-18: Task 7 completed the root seam migration by making `FolderCardPanel.svelte` a rune-based projection layer over `panelModel` with direct callback props for root outputs, including hydration-range forwarding via `onHydrateRange`.
- 2026-04-18: `FolderCardView.ts` now uses `mount/unmount` instead of `new/$on/$destroy`, with host-owned callback wiring and no ordinary-state remounts; state updates continue through `panelModel.mutate(...)`.
- 2026-04-18: Node-lane host-contract tests remain stable after the seam swap by mapping callback props back to existing `panelEventHandlers` in mocks, preserving legacy test payload invocation shape (`handler({ detail })`).
- 2026-04-18: Task 8 removed `compatibility.componentApi = 4` from both `esbuild.config.mjs` and the jsdom Svelte plugin setup in `vitest.config.ts`, and build/tests stay green on standard Svelte 5 mount/unmount runtime paths.
- 2026-04-18: Remaining node-lane mock surfaces were updated to non-legacy cleanup semantics (`destroy` + mocked `unmount`) while preserving panel callback wiring and panel-model snapshot assertions in `card-context-actions.test.ts`.

- 2026-04-18: Task 8 evidence capture for the legacy API grep must preserve literal `\$set/\$on/\$destroy` text (for example with a single-quoted heredoc); otherwise shell expansion can corrupt the recorded command string in evidence artifacts.
- 2026-04-18: Task 9 added a single-job GitHub Actions workflow at `.github/workflows/ci.yml` on `ubuntu-latest` with Node `20`, `npm ci`, and the required verification command order (`npx svelte-check --tsconfig ./tsconfig.json`, `npm run check`, `npm run build`, `npm test`).
- 2026-04-18: Task 9 unblock fix keeps runtime unchanged by using a template-safe sort separator type guard in `Toolbar.svelte` and by binding `projectCardsToRows` row typing to `NoteCardRecord` in `FolderCardPanel.svelte`, allowing the exact local verification chain to complete.

- 2026-04-18: Plan-compliance audit confirms Tasks 1-9 deliverables are present in the worktree: split Vitest node/jsdom lanes, real Svelte runtime tests for `CardItem.svelte`/`Toolbar.svelte`/`FolderCardPanel.svelte`/`FolderCardView.test.ts`, `src/view/panel-model.ts`, Svelte 5 `mount/unmount` host wiring, compatibility removal in `esbuild.config.mjs`, and CI in `.github/workflows/ci.yml`.
- 2026-04-18: Strict audit re-ran `npx svelte-check --tsconfig ./tsconfig.json && npm run check && npm run build && npm test` plus the four targeted runtime tests; all passed, with only the already-documented non-blocking `Toolbar.svelte` a11y warnings remaining.

- 2026-04-18: Final runtime QA reran `npm run check && npm run build && npm test`; the full chain passed (14 test files / 234 tests), including jsdom runtime suites that exercise empty state, card and toolbar interactions, hydrate-range callback wiring, and onClose teardown cleanup.
- 2026-04-18: Final Wave F4 scope-fidelity cleanup removed out-of-scope worktree noise (`.sisyphus` non-migration plans/notepads/drafts, `.serena/memories` non-deliverable artifacts, and `.opencode/skills/project-docs-maintenance`) while preserving migration-relevant source/test/mock/CI and `svelte-5-migration` plan/notepad/evidence surfaces.
- 2026-04-18: Final Wave F2 quality fix removed the legacy-like node-lane facade in `card-context-actions.test.ts` by wiring `vi.mock("svelte")` to explicit `svelteMountMock`/`svelteUnmountMock`, mounting via `createMountedPanel(...)`, and asserting `unmount(component)` instead of spying on component `.destroy()`.
- 2026-04-18: `src/svelte.d.ts` now uses Svelte 5 `Component` typing instead of deprecated `SvelteComponentTyped`, keeping the wildcard declaration minimal while preserving `svelte-check` resolution for the dynamic `FolderCardPanel.svelte` import seam.
- 2026-04-18: Project docs now record the migration as complete, not transitional: `docs/START_HERE.md` and `docs/architecture.md` describe the standard Svelte 5 `mount/unmount + panel-model + callback props` seam, and `docs/decisions/2026-04-18-finish-svelte-5-host-and-component-seam.md` links the completed state back to the 2026-04-03 compatibility-stage decision.
