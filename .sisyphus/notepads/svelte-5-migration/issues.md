- 2026-04-18: `npx svelte-check --tsconfig ./tsconfig.json` initially failed on `./FolderCardPanel.svelte` declaration resolution from `FolderCardView.ts`, even though `tsc --noEmit` passed.
- 2026-04-18: tsconfig-only attempts (`typescript-svelte-plugin`, `allowArbitraryExtensions`, and type-list adjustments) did not clear that specific svelte-check error.
- 2026-04-18: Smallest reliable unblock was adding `src/svelte.d.ts` with a targeted wildcard module declaration (`*FolderCardPanel.svelte`) returning `typeof SvelteComponentTyped`; this resolves svelte-check without touching runtime production files.
- 2026-04-18: Existing Toolbar a11y warnings are still reported by both svelte-check and production build; they are pre-existing and non-blocking for Task 1 baseline.
- 2026-04-18: `Toolbar.svelte` menu assertions initially raced the DOM portal actions in jsdom; adding `await tick()` after menu-opening clicks stabilizes filter/sort/folder interaction checks without changing production component code.
- 2026-04-18: `FolderCardView.test.ts` initially failed under Vitest because `vi.mock` factories are hoisted and cannot capture top-level class declarations; resolved by moving mock classes into `vi.hoisted` state.
- 2026-04-18: `onClose()` leaves the `.folder-card-view` host wrapper in the container while destroying panel internals; host-contract assertions should check for stale `.fce-shell` teardown and duplicate event behavior instead of assuming wrapper removal.
- 2026-04-18: While migrating Task 4 to model-driven updates, calling `pushState()` inside `onOpen()` caused behavioral regressions in node-lane bulk-selection tests (early reconciliation/derived-card recomputation); fixed by seeding model state from current host fields without forcing `deriveVisibleCards()` on open.
- 2026-04-18: Task 5 verification surfaces Svelte 5 deprecation warnings for legacy `on:*` directives in `CardItem.svelte`; warnings are non-blocking for current migration scope because Task 5 targets prop/event contracts rather than event-attribute syntax migration.
- 2026-04-18: Task 6 still reports pre-existing Toolbar a11y warnings during `vitest`/`build` (`menuitem` tabindex and static click-interaction warnings in folder-tree nodes); they remain non-blocking and intentionally out of scope for this contract-focused migration.

- 2026-04-18: Initial Task 7 panel rewrite triggered Svelte `state_referenced_locally` warnings by reading `panelModel` in `$state(...)` initializer; resolved by initializing with `EMPTY_PANEL_STATE` and syncing from `panelModel` inside `$effect`.
- 2026-04-18: Task 8 removal of compatibility mode did not introduce new failures; build/test still emit the same pre-existing Toolbar a11y warnings (`menuitem` tabindex/static click interaction), which remain out of scope and non-blocking.
- 2026-04-18: Task 9 local verification chain currently blocks at `npx svelte-check --tsconfig ./tsconfig.json` with TypeScript errors in `src/view/Toolbar.svelte` (separator union narrowing around `SORT_OPTIONS`) and `src/view/FolderCardPanel.svelte` (`RowProjectionCard` missing `hydrated`), so the full `svelte-check && check && build && test` chain cannot complete in this scope.
- 2026-04-18: Task 9 blocker resolved after minimal type-only fixes; verification now passes while preserving the same pre-existing non-blocking Toolbar a11y warnings during `svelte-check`/`build`/`test`.

- 2026-04-18: Final runtime QA still reports the same non-blocking Svelte a11y warnings in `src/view/Toolbar.svelte` (menuitem tabindex and static click interaction at lines 606 and 617) during `npm run build` and `npm test`; no runtime failures were observed in the verification chain.
- 2026-04-18: Final Wave F4 rejection root cause was changed-file-surface noise from unrelated plan/notepad/memory/skill artifacts; resolved by removing only out-of-scope artifacts and re-auditing with `git status --short && git diff --name-only --cached && git diff --name-only`.
