- Verification caught two Task 1 regressions: `package-lock.json` root version drifted out of scope and `applySearchFilter()` still appended pipeline-side non-Markdown title matches. Reverted the lockfile version-only diff and made indexed-ready `orderedPaths` fully authoritative so focused tests and `npm run check` pass.
- Atlas verification caught a remaining Task 2 production violation in `FolderCardView.ts`: search-status gating still inferred storage-unavailable from `health.detail`. Fixed the view to use explicit `health.persistence` / `health.rebuildReason` instead of detail-string matching.
- Task 7 verification needed one more runtime-boundary fix: the plugin source originally dropped uncategorized TFiles, which excluded the plan’s explicit Assets/Project Brief.pdf example. The final policy now treats any supported TFile as indexable, using title-only docs for non-Markdown while keeping cachedRead limited to Markdown files.
- Task 8 rejection fix: healthy persisted-index restore originally still called `refreshDocumentStateFromSource()` inside `SearchIndexManager.restore()`, so `initializeSearchService()` could trigger a vault scan during `onload()`. The fix leaves restored MiniSearch query-capable immediately and moves reconciliation exclusively behind the deferred `syncDocumentStateFromSource()` path after layout ready.

- Task 14 audit found an ambiguous documentation remnant in `docs/plan/v1-development-plan.md`: its old Phase 3 planning text still described fallback search as if it were actionable current guidance. Fixed by adding explicit CAUTION/NOTE markers that the section is historical only and that current behavior is indexed-only blocked search; also corrected `docs/architecture.md` to reflect supported non-Markdown title/basename-only indexed matches.

- F2 final verification rejection fix: restored MiniSearch instances were query-ready before deferred `syncDocumentStateFromSource()`, but mutation cleanup still trusted `documentsByPath`. Pre-sync delete/file-rename could therefore skip `index.discard()` and leave stale restored paths, while persisted mutation counts could drift to zero. The fix keeps startup scan-free restore intact and routes mutation cleanup/counting through MiniSearch live ids/documentCount instead of the deferred runtime map.
- Follow-up review fix: safe folder-prefix rewrites from restored MiniSearch metadata also need to publish rebuild-required snapshot health when rewrite safety fails (for example, path collisions). Without that emitted state, fire-and-forget vault mutation handling can leave the search service marked ready even though a rebuild is required.

- Fix release validation failure: `npm run check:svelte` failed because `emptyStateMessage` was missing from `EMPTY_PANEL_STATE` in `FolderCardPanel.svelte`. Fixed by adding `emptyStateMessage: ""` as the default empty state, maintaining sync with `PanelModelState`.
