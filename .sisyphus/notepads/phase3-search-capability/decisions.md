- 2026-04-18: Finalized readiness seam contract without ownership redesign. Plugin-global lifecycle + health snapshot remain service-owned in `main.ts`; per-view query ownership remains runtime-only and outside settings.
- Added explicit rename classification semantics: `file`, `folder-safe-prefix-rewrite`, `folder-rebuild-required`; folder rename must use safe prefix rewrite only when provable, else rebuild-required fallback/building path.
- Extended `SearchQueryResult`/`SearchServiceSnapshot` contract with execution + health metadata while keeping score metadata runtime-internal and out of render-facing card types.
- 2026-04-18 Task 2: Added minimal compile-safe `IndexStore`, `SearchIndexManager`, and `IndexedSearchService` seams intentionally limited to contract-safe placeholder behavior; deferred real IndexedDB/MiniSearch backend implementation to later tasks.
- 2026-04-18 Task 3: Expanded `IndexStoreNamespaceMetadata` with `documentCount`/`lastIndexedAt` and require version-drift/corrupt branches to clear persisted state while returning typed `rebuild-required` outcomes.
- 2026-04-18 Task 4: Kept `SearchIndexManager` query-independent and adapter-driven (`documentSource`) so full build/restore/mutation behavior stays isolated from view/UI wiring and Obsidian globals.
- Task 4 mutation persistence policy: after each applied incremental change, manager serializes and writes the updated index snapshot so restore-time metadata stays coherent with in-memory state.
- 2026-04-18 Task 5: kept document-preparation layer pure under `src/search/` with explicit input DTOs and no view/plugin ownership, and reused markdown normalization utilities rather than introducing a second parsing pipeline.
- 2026-04-18 Task 5: folder rename classification uses conservative safe-prefix checks; overlapping/ambiguous prefix rewrites are escalated to `rebuild-required` to avoid stale path mappings.

- 2026-04-18 Task 7: Recovery notices are boundary-gated in plugin host wiring (degraded once, recovered once) to avoid per-mutation spam while still surfacing rebuild/failure transitions globally.
