# Task Completion Checklist

After completing a coding task, run:

1. `npm run check` — TypeScript type-checking (tsc --noEmit, strict mode)
2. `npm run build` — Production build to verify bundling succeeds

There is no linter, formatter, or test suite configured.
